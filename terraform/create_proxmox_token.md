Datacenter → Permissons → Api Tokens → Add | Named ``terraform``

Choose your terraform user in ``User`` field, write some descriptions for ``Token ID`` and click ``Add``.

Save your ``Token ID`` and ``Secret`` to keepass.
